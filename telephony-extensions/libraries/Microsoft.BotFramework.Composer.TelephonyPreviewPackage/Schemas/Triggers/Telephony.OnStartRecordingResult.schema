{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": [ "implements(Microsoft.ITrigger)", "extends(Microsoft.OnCondition)" ],
  "title": "On Recording Start Result",
  "description": "Triggered when a Telephony Recording Start command result activity is received",
  "type": "object",
  "required": [
      "actions"
  ]
}